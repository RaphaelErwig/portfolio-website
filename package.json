{
  "name": "portfolio-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "node scripts/check-build.mjs",
    "verify": "astro build && node scripts/check-build.mjs"
  },
  "dependencies": {
    "astro": "^7.1.1"
  }
}
